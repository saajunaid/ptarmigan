# Agent Docs

This folder is user-owned at runtime.

The extension ships only this `README.md` placeholder to avoid overwriting project-specific artefacts (for example, `ARTIFACTS.md`, handoff notes, and stage outputs) during install/update.
